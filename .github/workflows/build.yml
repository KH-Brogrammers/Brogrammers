name: Build Artifacts

on:
  workflow_dispatch:
  repository_dispatch:

jobs:
  Build:
    name: Build and Package
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Use Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '22.x'
        cache: 'npm'
    
    - run: npm ci
    - run: npm run build --if-present
    
    - name: Make build script executable
      run: chmod +x ./.github/scripts/build.sh
    
    - name: Create deployment package
      run: ./.github/scripts/build.sh
    
    - name: Final Security Verification
      run: |
        echo "🔍 FINAL SECURITY SCAN - Ensuring no source code in artifact..."
        if find "deployment-package" \( -name "*.ts" -o -name "*.tsx" -o -name "*.jsx" -o -name "src" \) 2>/dev/null | grep -q .; then
          echo "❌ DEPLOYMENT BLOCKED: Source code detected!"
          exit 1
        fi
        echo "✅ SECURITY CLEARED: Artifact contains only built files"
    
    - name: Upload deployment artifact
      uses: actions/upload-artifact@v4
      with:
        name: deployment-build
        path: deployment-package/
        retention-days: 3
    
    - name: Build Complete
      run: echo "🎉 Build completed successfully! Artifact ready for deployment."